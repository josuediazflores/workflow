{
  "$comment": [
    "Which `workflows/99_e2e` fixtures this app implements. See",
    "`packages/core/e2e/e2e-conformance.example.json` for the format and",
    "`ConformanceConfig` in `packages/core/e2e/utils.ts` for the gate.",
    "",
    "Grow this list as fixtures are ported to `workflows/99_e2e.py`; never",
    "shrink it without saying why. A name here that the app stops registering",
    "fails the run rather than quietly skipping."
  ],
  "language": "python",
  "fixtures": [
    "addTenWorkflow",
    "nullByteWorkflow",
    "parallelSleepWorkflow",
    "promiseAllWorkflow",
    "sleepInLoopWorkflow",
    "sleepingWorkflow"
  ],
  "unsupported": {
    "resilient start: addTenWorkflow completes when run_created returns 500": "vercel-py's workflow handler reads the run row before replaying (runtime.py:529 `world.runs_get`) and 500s when it is absent. The TypeScript runtime instead bootstraps the run from `run_started` using the input carried in the queue message, which is what makes a run survive a failed `run_created`."
  }
}
